apiVersion: apps/v1
kind: Deployment
metadata:
  name: alphaforge-auditor
  namespace: alphaforge
  labels: { app: alphaforge-auditor }
spec:
  replicas: 1
  selector: { matchLabels: { app: alphaforge-auditor } }
  template:
    metadata: { labels: { app: alphaforge-auditor } }
    spec:
      containers:
        - name: auditor
          image: ghcr.io/alphaforge/auditor:latest
          envFrom:
            - configMapRef: { name: alphaforge-config }
            - secretRef:    { name: alphaforge-secrets }
          resources:
            requests: { cpu: "300m", memory: "512Mi" }
            limits:   { cpu: "2",    memory: "1Gi"   }
