{
  "buildCommand": "cd frontend && npm install && npx vite build --outDir ../dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/_/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
